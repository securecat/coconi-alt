# 更新履歴

このファイルには、プロジェクトのすべての重要な変更が記録されています。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョン管理は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [1.3.0] - 2026-07-14

### 追加

- オプションページの見た目設定セクションに「初期値に戻す」ボタンを追加。入力欄に初期値が入るだけで、反映には「保存」の操作が必要

## [1.2.0] - 2026-07-14

### 追加

- パネルモードの「パネル背景の不透明度」設定を追加（デフォルト：100%）。100未満にすると背景が半透明になり、元の画像が透けて見える。altチェック用途向けで、テキストは常に不透明のまま表示される
- 不透明度が100未満のとき、パネルのテキストに4方向のハロー（縁取り）を自動付与。前景色の輝度に応じて白/黒を自動選択し、透けた画像の上でも文字を読みやすくする
- オプションページに GitHub Issues へのバグ報告リンクを追加

### 変更

- オプションページの全セクションを `fieldset`/`legend` の見た目に統一し、「パネル背景の不透明度」を独立したセクションに分離
- 設定保存メッセージの文字色を変更（ライト：#197A4B／ダーク：#4EC98A）

## [1.1.0] - 2026-07-12

### 追加

- CSS背景画像（`url()` を含む `background-image`）を非表示にする機能を追加。スクリーンリーダーに知覚されない背景画像を消すことで、「耳で見ている」世界の視覚化に忠実な表示にする（グラデーションのみの背景は対象外）
- オプションページに「背景画像（background-image）の扱い」設定を追加（非表示にする／そのまま表示。デフォルト：非表示にする）

### 変更

- フォント指定から `system-ui` を削除し `sans-serif` に統一（Windowsで本文がUI用フォント「Yu Gothic UI」で表示されるのを避けるため）
- ポップアップの横幅を拡大（280px → 420px）

## [1.0.0] - 2026-07-10

### 追加

- 初回リリース
